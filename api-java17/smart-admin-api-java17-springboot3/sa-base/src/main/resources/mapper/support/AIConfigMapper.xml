<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.base.module.support.ai.dao.AIConfigDao">

    <!-- 分页查询 -->
    <select id="queryPage" resultType="net.lab1024.sa.base.module.support.ai.domain.entity.AIConfigEntity">
        SELECT *
        FROM t_ai_config
        WHERE deleted_flag = false
        <if test="provider != null and provider != ''">
            AND provider = #{provider}
        </if>
        <if test="modelType != null and modelType != ''">
            AND model_type = #{modelType}
        </if>
        <if test="enabledFlag != null">
            AND enabled_flag = #{enabledFlag}
        </if>
        <if test="searchWord != null and searchWord != ''">
            AND (model_name LIKE CONCAT('%', #{searchWord}, '%')
            OR model_display_name LIKE CONCAT('%', #{searchWord}, '%'))
        </if>
        ORDER BY sort_order ASC, id DESC

    </select>

    <!-- 根据配置Key查询 -->
    <select id="selectByConfigKey" resultType="net.lab1024.sa.base.module.support.ai.domain.entity.AIConfigEntity">
        SELECT *
        FROM t_ai_config
        WHERE config_key = #{configKey}
          AND deleted_flag = false
        LIMIT 1
    </select>

    <!-- 查询默认模型 -->
    <select id="selectDefaultModel" resultType="net.lab1024.sa.base.module.support.ai.domain.entity.AIConfigEntity">
        SELECT *
        FROM t_ai_config
        WHERE default_flag = true
          AND enabled_flag = true
          AND deleted_flag = false
        LIMIT 1
    </select>

    <!-- 查询所有启用的配置 -->
    <select id="selectEnabledConfigs" resultType="net.lab1024.sa.base.module.support.ai.domain.entity.AIConfigEntity">
        SELECT *
        FROM t_ai_config
        WHERE enabled_flag = true
          AND deleted_flag = false
        ORDER BY sort_order ASC, id DESC
    </select>

    <!-- 根据提供商查询 -->
    <select id="selectByProvider" resultType="net.lab1024.sa.base.module.support.ai.domain.entity.AIConfigEntity">
        SELECT *
        FROM t_ai_config
        WHERE provider = #{provider}
          AND deleted_flag = false
        ORDER BY sort_order ASC, id DESC
    </select>

    <!-- 根据模型类型查询 -->
    <select id="selectByModelType" resultType="net.lab1024.sa.base.module.support.ai.domain.entity.AIConfigEntity">
        SELECT *
        FROM t_ai_config
        WHERE model_type = #{modelType}
          AND deleted_flag = false
        ORDER BY sort_order ASC, id DESC
    </select>

    <!-- 设置默认模型 -->
    <update id="updateDefaultFlag">
        UPDATE t_ai_config
        SET default_flag = #{defaultFlag},
            update_time  = NOW()
        WHERE id = #{id}
    </update>

    <!-- 清除所有默认标记 -->
    <update id="clearAllDefaultFlags">
        UPDATE t_ai_config
        SET default_flag = false,
            update_time  = NOW()
        WHERE default_flag = true
    </update>

</mapper>
